# Supabase (email auth)
VITE_SUPABASE_URL=https://fqkhykvfrtaszitksspy.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=

# Cloudflare Turnstile site key. Its matching secret must be configured in
# Supabase Auth → Bot and Abuse Protection.
VITE_TURNSTILE_SITE_KEY=

# SpeechC API — required for production/Vercel builds
# In local `npm run dev`, leave unset so Vite proxies /api → :8000
VITE_API_URL=https://api-production-fa6b.up.railway.app
